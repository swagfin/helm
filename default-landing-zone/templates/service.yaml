apiVersion: v1
kind: Service
metadata:
  name: default-landing-zone
  namespace: {{ .Values.namespace | quote }}
spec:
  selector:
    app: default-landing-zone
  ports:
    - name: http
      port: 80
      targetPort: 80
